# Pitfalls

Past traps, indexed by symptom. Preserve verbatim errors, root cause, workaround, and prevention
so the next occurrence is grep-able instead of re-debugged.

Deployment profile recorded during setup: none.
